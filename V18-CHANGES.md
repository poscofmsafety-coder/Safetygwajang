# v18 변경사항

## 메인/가독성
- 메인 대형 제일이의 고정 히어로 슬로건을 제거하고 캐릭터를 카드 중앙에 정돈했습니다.
- 제일이 클릭 시 랜덤 안전 응원문구와 모션은 유지했습니다.
- `취준생 시작하기`, `재직자 시작하기` 버튼의 화살표를 제거했습니다.
- 기본 글자 크기를 확대하고 모바일·레거시 실무 화면의 작은 글씨와 입력창도 함께 키웠습니다.

## KRAS 위험성평가
- KRAS 관련 버튼의 장식 화살표를 제거했습니다.
- 특정 연도에 고정된 KRAS 표현을 제거하고 `KRAS 기준`, `KRAS 사용설명서` 등 연도 중립 표현으로 통일했습니다.
- KRAS 공식 연결주소는 `https://portal.kosha.or.kr/kras/implement/real`을 유지합니다.
- 현장 외부정보는 사용자 화면에서 `데이터 불러오기`, `현장 참고자료` 등으로 표현해 기술 구현방식을 과도하게 노출하지 않습니다.

## 작업 데이터 관리
- 수리 결재 역할의 `실장` 표기를 `안전보건관리책임자`로 변경했습니다.
- 부식 결재 역할의 `부장` 표기를 `팀장`으로 변경했습니다.
- 작업 데이터 예시 Excel의 헤더도 같은 기준으로 변경했습니다.

## MSDS
- 사용자에게 불필요한 내부 검토절차 설명 상자를 제거했습니다.
- 실제 조회·관리 로직은 유지합니다.

## 법정 안전보건교육 관리
- 화면 제목을 `법정 안전보건교육 관리`로 명확히 변경했습니다.
- `현장명` 대신 `부서명`을 사용하고, 부서명 또는 근로자 이름으로 즉시 검색할 수 있습니다.
- 근로관계를 `원청/협력사`로 선택하며 협력사 선택 시 협력사명을 별도로 입력합니다.
- 교육강사는 `직책(관리감독자/안전관리자/직접 입력) + 성함`으로 관리합니다.
- `작업내용 변경 시 교육`을 근로자별 대상 지정 및 이수기록으로 추가했습니다.
- 특별교육은 근로자별로 여러 대상을 추가·삭제할 수 있고, `특별교육(밀폐공간 작업)`, `특별교육(전기작업)`처럼 대상작업을 직접 입력할 수 있습니다.
- 기존 브라우저 저장자료는 가능한 범위에서 새 구조로 마이그레이션합니다.
- CSV/JSON 내보내기에도 새 부서·강사·작업내용변경·특별교육 구조를 반영했습니다.
