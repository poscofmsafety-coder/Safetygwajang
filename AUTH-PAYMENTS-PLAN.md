# 회원가입 · 로그인 · 결제 전환 설계안

현재 버전에는 적용하지 않고, UI/기능 확정 후 붙일 수 있도록 권장 구조만 정리합니다.

## 1. 권장 서버 구조

- Cloudflare Worker: 인증 API, 결제 승인/웹훅, 권한 확인
- Cloudflare D1: 사용자, 로그인 연결, 구독/결제, 권한 데이터
- Cloudflare Turnstile: 회원가입/로그인/결제 요청의 봇 방어
- HttpOnly + Secure + SameSite 세션 쿠키: 브라우저 로그인 세션
- 비밀번호 회원가입을 제공할 경우 비밀번호 원문 저장 금지, 검증된 password hashing 사용

## 2. 로그인 방식

지원 가능:
- 이메일/아이디 + 비밀번호
- Google 로그인
- NAVER 로그인
- Kakao 로그인

소셜 로그인은 브라우저에서 받은 인증 결과를 그대로 신뢰하지 않고 Worker가 공급자 토큰/ID 토큰을 검증한 뒤 내부 사용자 ID에 연결합니다.

## 3. 권한 모델

권장 등급:
- FREE: 공개 취준 콘텐츠 + 일부 체험 기능
- MEMBER: 로그인 사용자, 개인 기록 동기화
- PRO: 유료 실무 기능 전체 이용
- ADMIN: 운영자

모든 유료 페이지는 단순히 프런트엔드에서 버튼만 숨기지 말고 `/api/entitlements` 또는 Worker route guard에서 서버측으로 구독 상태를 확인합니다.

## 4. 결제

1순위 권장: Toss Payments 최신 SDK/주문서형 또는 결제창형
- 카드
- 간편결제
- 계좌 기반 결제수단은 계약/상품 구성에 따라 추가

대안: PortOne V2를 사용해 여러 PG/간편결제를 한 인터페이스로 관리

Naver Pay를 직접 붙이고 싶으면 별도 가맹/연동 조건과 키 발급이 필요할 수 있으므로 계약 완료 후 실제 키 기준으로 붙입니다.

## 5. 유료 접근 흐름

1. 사용자 로그인
2. 결제 주문 생성 (`pending`)
3. PG 결제창 호출
4. Worker에서 서버측 결제 승인 검증
5. 웹훅으로 최종 결제 상태 재확인
6. D1 `subscriptions`/`entitlements` 갱신
7. PRO 페이지/API 요청마다 서버측 권한 확인
8. 환불/취소/만료 웹훅 수신 시 즉시 권한 회수

## 6. 광고 + 유료 모델

권장:
- FREE 사용자: AdSense 중심
- PRO 사용자: 광고 제거 또는 최소화 + 실무 기능 전체 제공
- 결제/로그인/개인정보 입력 화면: 광고 배치하지 않음

다른 광고 네트워크도 기술적으로 병행할 수 있지만, 승인 전부터 여러 광고 스크립트를 동시에 넣기보다는 AdSense 승인/트래픽을 먼저 안정화한 후 매체별 정책과 수익성을 비교하는 편이 안전합니다.
