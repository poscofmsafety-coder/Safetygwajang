# KRAS 위험성평가 구현 기준 (v14)

기준 검토일: 2026-09-05

## 1. 기준자료

- 안전보건공단 `위험성평가시스템(KRAS) 사용설명서` 2025.1.2
- 고용노동부고시 제2024-76호 `사업장 위험성평가에 관한 지침` (시행 2025-01-02)
  - https://www.law.go.kr/LSW/admRulInfoP.do?admRulSeq=2100000251014
- 안전보건공단 위험성평가 안내자료 / 3단계 판단법 자료
  - https://www.kosha.or.kr/
- KRAS 공식사이트
  - https://portal.kosha.or.kr/kras/implement/real

## 2. 구현 원칙

현행 고시와 KRAS 흐름에 따라 다음 단계를 한 화면에서 관리합니다.

1. 사전준비
2. 유해·위험요인 파악
3. 위험성 결정
4. 위험성 감소대책 수립·실행 및 재평가
5. 기록·공유·TBM

지원 평가방법은 다음과 같습니다.

- 위험성 수준 3단계 판단법
- 체크리스트법
- 핵심요인기술법(One Point Sheet, OPS)
- 빈도·강도법

3단계 판단법의 기본 문구는 안전보건공단 안내의 예시를 참고하되, 허용 가능한 위험성 수준은 사업장이 사전에 정한 실시규정을 사용하도록 설계했습니다. 빈도·강도법도 전국 공통 점수구간을 강제하지 않고 사업장 허용점수를 직접 설정하도록 했습니다.

유해·위험요인 파악방법에는 사업장 순회점검, 근로자 상시 제안, 설문·인터뷰, MSDS·작업환경측정·건강진단 등의 안전보건 자료, 체크리스트, 재해·아차사고 등을 반영했습니다.

감소대책은 제거 → 대체 → 공학적 대책 → 관리적 대책 → 개인보호구 순으로 검토하도록 구현했고, 대책 완료 후 잔여위험 재평가 여부를 등록 준비 점검에 포함했습니다.

## 3. KRAS Excel 호환성에 대한 중요한 제한

2025 KRAS 사용설명서를 검토한 결과:

- 일반 위험성평가(3단계, 체크리스트, OPS, 빈도·강도)의 **Excel 일괄 직접 업로드 기능은 공식 설명서에서 명시적으로 확인되지 않았습니다.**
- 산업보건 위험성평가는 평가결과를 PDF/EXCEL로 출력할 수 있고 완료된 결과를 KRAS에 등록하는 흐름이 안내되어 있습니다.
- 화학물질 위험성평가는 KRAS가 제공하는 **별도 Excel 일괄 업로드 기능**이 명시되어 있습니다.
- 일반 위험성평가 결과 보기에서는 평가 과정 파일을 등록/첨부하는 기능이 안내되어 있습니다.

따라서 안전과장의 `KRAS 등록보조 Excel`은 다음 용도로 제공합니다.

- KRAS 화면 입력 순서에 맞춘 작업파일
- 복사/붙여넣기용 정리자료
- 평가 과정 증빙 첨부자료
- 내부 검토/보존용 보고자료

일반 위험성평가의 직접 데이터 import를 보장하는 파일이라고 표시하지 않습니다. 화학물질은 KRAS 공식 Excel 양식을 사용해야 합니다.

## 4. 데이터 저장/이관

- 신규 저장키: `sgw_kras_v1`
- 기존 `sgw_v5_risk-execute` 기록은 원본을 삭제하지 않고 선택적으로 가져올 수 있습니다.
- 순회점검에서 `수시 위험성평가 연계`를 선택하면 신규 KRAS 저장키의 유해·위험요인으로 자동 등록되고 `재검토 필요` 상태로 생성됩니다.
- 브라우저 localStorage 기반이므로 중요한 기록은 JSON 백업 또는 Excel 내보내기로 별도 보존해야 합니다.

## 5. 출력

- `KRAS 등록보조 Excel(.xlsx)` : 브라우저에서 SheetJS 공식 CDN 사용 가능 시 XLSX로 생성
- SheetJS 연결 실패 시 Excel 2003 호환 `.xls`로 자동 대체
- `KRAS 복사용 TSV`
- 전체 JSON 백업/복원
- 빈 Excel 양식: `assets/kras-registration-helper-template.xlsx`


## v15 추가: 기존 KRAS 자료 가져오기 + Groq AI 초안
- `worker/risk-assessment.html`에 SMART START 영역을 추가했습니다.
- 기존 KRAS/위험성평가 Excel(`.xlsx`, `.xls`), CSV, TSV, JSON을 업로드하면 시트와 헤더를 자동 탐색하여 `sgw_kras_v1` 구조로 변환합니다.
- 지원 헤더 예: 공정명/작업명, 유해·위험요인, 현재 안전보건조치, 위험성 수준, 가능성·중대성, 감소대책, 담당자, 완료예정일, 개선 후 위험성 등입니다.
- 병합셀로 인해 공정명이 다음 행에서 공란인 경우 직전 공정명을 이어받도록 처리합니다.
- 가져온 자료는 원문 값을 최대한 보존하지만 `needsReview=true`로 저장해 현장조건과 현재 사업장 실시규정을 다시 확인하게 합니다.
- `/api/ai/kras`는 기존 `GROQ_API_KEY`를 서버에서만 사용하며 브라우저에 키를 노출하지 않습니다.
- Groq Structured Outputs(JSON Schema strict)를 사용해 위험 시나리오, 현재조치, 잠정 위험수준, 감소대책, 잔여위험, 현장 확인사항을 구조화합니다.
- AI는 입력되지 않은 현장조건·보호장치·측정값·법규충족 여부를 사실처럼 생성하지 않도록 프롬프트와 후처리에서 제한합니다.
- AI 결과는 항상 검토용이며 자동 확정하지 않습니다. 특히 빈도·강도법 숫자점수는 사업장 실시규정이 있어야 하므로 AI가 임의 확정하지 않습니다.
